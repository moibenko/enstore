###if [ -z "${PS1-}" ] ; then return; fi
# Source global definitions
###if [ -f /etc/bashrc ]; then
###        . /etc/bashrc
###fi

if [ -f /etc/bashrc ]; then
        . /etc/bashrc
fi

set +u
source /usr/local/etc/setups.sh
set -u
